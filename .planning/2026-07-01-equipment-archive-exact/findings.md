# Findings

- 导出包源页面为 `https://sms.axjyun.com/equip/record`，基准视口 1912×901，另含 1440、768、390 宽度截图。
- 页面由 200px 固定侧栏、64px 顶栏、48px 页签栏与内容区构成；窄屏下不收起侧栏，主内容发生横向裁切。
- 主色为 `#129BFF`，链接/强调蓝同时出现 `#2D8CF0`；正文为 `#515A6E`，边框 `#DCDEE2`，表格斑马纹 `#F4F5F7`。
- 控件约 34px 高，表头约 38px，数据行约 48px；正文主字号 14px。
- 导出包没有图片资源，仅有字体文件；源图中的图标、开关、单选框和 Logo 可稳定使用 SVG、CSS 或 Element 组件实现。
- 工作区已有 `src/prototypes/untitled-4` 装备迭代版且存在未提交修改；本次新建独立目录，避免覆盖。

