# Findings

- 目标原型为 `src/prototypes/equipment-archive-exact`，当前已有未提交修改，必须增量编辑。
- 现有 `inventory-check-entry` 已有“一物一码”入口占位和实际库存回写位置。
- 视觉事实源为用户提供的两张弹窗截图与原型内 `DESIGN.md`。
- Product Design 保存上下文为空，本次直接使用当前对话和仓库内事实源。
- 现有 Vue 原型将页面、状态与交互集中在 `vue-app.ts`，React `index.tsx` 负责 Hash Page 和批注路由。
- 进行中的示例盘点仓库是“历城分公司仓库”，适合用“强光手电”作为一物一码演示装备并保留现有盘点主流程。
- 页面间状态应留在同一个 Vue 挂载实例内；Hash 路由用于直接预览，内部进入扫码页不触发 React 重挂载，以保证回写状态不丢失。
- Edge 已成功打开 Page 1，输入框默认处于焦点状态，截图结构中的装备信息、扫码区、统计、四列表格和底部确认操作均完整渲染。
- 浏览器控制台唯一 error 为既有 `favicon.ico` 404；Vue 的 `Property undefined` 警告来自当前整段运行时模板的既有编译方式，页面功能仍正常渲染。
- Page 1 连续扫描新码后记录即时置顶、数量由 3 增至 4，输入框保持焦点；再次扫描同码后重复计数为 1，扫描已登记在其他装备的码后冲突计数为 1，列表数量未变化。
- Page 2 初始正确显示匹配 2、新增 1、冲突 1，实盘为 3、差异为 0；新增一个匹配码和一个未知码后实盘为 5、差异为 +2，重复扫描未知码未重复计数。
- “本次新增”筛选只保留待建档记录；删除其中一条后返回盘点录入，强光手电实际库存回写为 4、差异为 +1。
- 提交盘点确认弹窗正确汇总 1 项库存差异与 1 个待建档物码。
