# Findings

- 采购管理当前包含采购需求、采购计划、采购订单三类单据。
- 现有需求与计划编号为三位流水，如 `XQ20260616001`、`JH20260616001`。
- 现有订单编号采用带分隔符的 `CGDD-yyyyMMdd-####`，需统一为 `DDyyyyMMdd####`。
- 编号同时存在于主数据、分配关系、入库来源、追溯关系和组件示例中，需要整体迁移。
- 新生成采购订单原先混入计划尾号、随机种子和连字符；现改为读取当日已有订单最大流水并递增。
